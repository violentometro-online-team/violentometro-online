# Violentómetro Online Server

A simple webserver made in Flask to handle the model and its requests separately from the app.

## Requirements

- Having the model in a readable location.

## How to run

```sh
python server.py
```
